# MT3 RunPod Worker Dependencies
# Note: Most dependencies are installed directly in Dockerfile
# with pinned versions for compatibility.
# This file is kept for reference only.

# Core dependencies are installed in Dockerfile:
# - numpy==1.23.5
# - tensorflow==2.11.0
# - jax==0.4.8 + jaxlib==0.4.7+cuda11.cudnn86
# - flax==0.6.10
# - t5x (from git)
# - mt3 (from git)
